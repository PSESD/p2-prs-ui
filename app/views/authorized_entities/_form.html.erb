<%= simple_form_for @authorized_entity, wrapper: :horizontal_form do |f| %>
	<%= f.error_notification %>

	<div class="form-inputs">
		<%= f.input :authorizedEntityName, label: "Name" %>
	</div>

	<div class="form-actions">
		<%= f.button :submit %>
	</div>
<% end %>
